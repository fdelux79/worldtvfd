FROM python:3.12-slim

WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends tesseract-ocr && rm -rf /var/lib/apt/lists/*
RUN pip install --no-cache-dir pytesseract Pillow

COPY livetvarm.so .
COPY runarm.py .

EXPOSE 7860

CMD ["python", "runarm.py"]
